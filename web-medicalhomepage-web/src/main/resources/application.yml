server:
  port: 8200
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/
spring:
  application:
    name: web-medicalhomepage-web

  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
    password:
    timeout: 500
    pool:
    max-active: 20
    max-wait: -1
    max-idle: 8
    min-idle: 0
  mongodb:
    uri: mongodb://name:password@//192.168.101.64:27017/test
